{
  "name": "no-sleep-browser-extension",
  "version": "1.0.0",
  "type": "module",
  "description": "A browser extension that prevents your computer from sleeping by keeping the browser active in the background. Works with Chrome, Edge, and other Chromium-based browsers. Includes sound notifications for battery alerts.",
  "main": "src/background.js",
  "scripts": {
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "format": "prettier --write src/**/*.js src/**/*.html *.json",
    "check": "eslint src/**/*.js && prettier --check src/**/*.js src/**/*.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.7.4"
  }
}
